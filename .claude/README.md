# `.claude/` — Claude Code project config

This directory holds project-scoped Claude Code config:

- `commands/` (future) — project-specific slash commands
- `settings.json` (future) — permissions, hooks, env vars scoped to this repo

The primary AI context file lives at the repo root as `CLAUDE.md` — that's the entry point. This directory is for tooling configuration, not narrative context.
